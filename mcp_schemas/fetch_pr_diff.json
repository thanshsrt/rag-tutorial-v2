{
  "name": "fetch_pr_diff",
  "description": "Fetches a GitHub pull request diff for review",
  "inputSchema": {
    "type": "object",
    "properties": {
      "pr_url": {"type": "string", "format": "uri"}
    },
    "required": ["pr_url"]
  }
}